/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.unsplash.com", "unsplash.com", "assets.aceternity.com"], // ✅ Add Unsplash as an allowed domain
      },
};

export default nextConfig;
